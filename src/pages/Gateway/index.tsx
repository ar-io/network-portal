import GatewayHeader from './GatewayHeader';

const Gateway = () => {

  const gatewayName = 'Loading...';

  return (
    <div>
      <GatewayHeader gatewayName={gatewayName}/>
    </div>
  );
};

export default Gateway;
